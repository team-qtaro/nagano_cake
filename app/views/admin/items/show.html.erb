<h3>商品詳細</h3>

<%= attachment_image_tag @item, :image, size: "100x100" %>

<p>商品名                 <%= @item.name %></p>
<p>商品説明               <%= @item.description %></p>
<p>ジャンル               <%= @item.genre.genre_name %></p>
<p>税込価格（税抜き価格)  <%= @item.add_tax_price.to_s(:delimited) %>(<%= @item.price.to_s(:delimited) %>)円</p>
<p>販売ステータス        	<% if  @item.is_stopped == true %>
                          <p class="text-secondary">販売停止中</p>
			                    <% else %>
			                    <p class="text-success">販売中</p>
			                    <% end %></p>   


